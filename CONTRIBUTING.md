# 协作约定

1. 从最新 `main` 创建个人功能分支，不直接在 `main` 开发。
2. 一个分支只解决一个明确任务，分支名称使用 `feature/`、`fix/`、`docs/` 前缀。
3. 提交前运行测试，并确认没有提交账号、密码、真实交易数据和大文件。
4. Commit 保持小而完整，使用 `feat:`、`fix:`、`test:`、`docs:`、`refactor:` 等前缀。
5. 通过 Pull Request 合并，由另一位同事审阅后再合并。

