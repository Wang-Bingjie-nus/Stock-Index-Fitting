"""Order-generation placeholder."""

import pandas as pd


def generate_orders(target_positions: pd.DataFrame, actual_positions: pd.DataFrame) -> pd.DataFrame:
    """Generate future rebalance orders."""
    raise NotImplementedError("Order generation has not been implemented.")

